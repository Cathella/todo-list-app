const displayTodoList = () => {
  return "Display the list";
};

const addTodo = () => {
  return "Adding a todo task";
};

const removeCompletedTasks = () => {
  return "delete completed tasks";
};

export { addTodo, removeCompletedTasks, displayTodoList }
